package gdg.devfest.app.ui.phone;

/**
 * A single-pane activity that shows a {@link SessionsFragment} containing a
 * list of sessions. This is used when showing the sessions for a given time
 * slot, or showing session search results.
 */
public class SessionsActivity extends
		com.google.android.apps.iosched.ui.phone.SessionsActivity {
}
